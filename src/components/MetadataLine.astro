---
import ArticleTypeLabel from './ArticleTypeLabel.astro';
import TopicPill from './TopicPill.astro';
import { formatDate, type ArticleType } from '../utils/articles';

interface Props {
  type: ArticleType;
  date: string | Date;
  readingTime?: string | number;
  topics?: string[];
  author?: string;
  separator?: string;
}

const { type, date, readingTime, topics = [], author, separator = '·' } = Astro.props;
const dateLabel = formatDate(date);
const timeLabel =
  readingTime !== undefined
    ? typeof readingTime === 'number'
      ? `${readingTime} min read`
      : readingTime
    : null;
---

<p class="metadata-line label">
  <ArticleTypeLabel type={type} />
  <span class="sep" aria-hidden="true">{separator}</span>
  <time datetime={typeof date === 'string' ? date : date.toISOString()}>{dateLabel}</time>
  {timeLabel && (
    <>
      <span class="sep" aria-hidden="true">{separator}</span>
      <span>{timeLabel}</span>
    </>
  )}
  {author && (
    <>
      <span class="sep" aria-hidden="true">{separator}</span>
      <span>{author}</span>
    </>
  )}
  {topics.length > 0 && (
    <span class="topics">
      {topics.map((topic) => (
        <TopicPill topic={topic} />
      ))}
    </span>
  )}
</p>

<style>
  .metadata-line {
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.35rem 0.5rem;
    line-height: 1.4;
    color: var(--faint);
  }

  .metadata-line :global(.type-label) {
    color: var(--muted);
  }

  .sep {
    color: var(--border);
  }

  .topics {
    display: inline-flex;
    flex-wrap: wrap;
    gap: 0.35rem;
    margin-left: 0.15rem;
  }
</style>
